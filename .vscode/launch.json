{
  "version": "1.0.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch local clone test",
      "program": "${workspaceFolder}/lib/cli.js",
      "args": ["clone", "localhost", "unit", "localhost", "unit2"]
    }
  ]
}
